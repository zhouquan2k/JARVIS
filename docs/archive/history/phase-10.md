AI 聊天程序进阶功能设计方案
基于“操作收敛于右侧面板”以及“偏向桌面端生产力习惯”的基调，本方案重点优化长对话的管理效率与输入体验。

一、 用户交互与界面设计 (UX/UI Design)
1. 右侧面板：对话管理中心 (Right Panel)
右侧面板作为全局的大纲与管理中枢，承载导航、标记与精简上下文的功能。

基础视图：列表项仅展示用户提问的前两行截断文本，保持界面紧凑。

悬停交互 (Hover)：当鼠标悬停在某个问题条目上时，条目右侧浮现操作图标组（包含“星标”与“删除”）。

星标操作 (Star)：

点击星标后，右侧面板中的图标变为实心高亮状态。

主视图联动：主聊天视图中对应的问答对卡片出现视觉变化（例如卡片边缘高亮或增加星标徽章），暗示该内容已被标记。

面板顶部可提供“全部”与“仅看星标”的切换标签，方便快速检索核心信息。

删除操作 (Delete)：

防误触：为保持心流，放弃全局遮罩弹窗，采用内联确认。点击删除图标后，该列表项文本临时变为“确认删除此问答对？”并附带确认/取消按钮。

主视图联动：确认删除后，该条目从右侧面板消失。主视图中对应的问答对触发高度收缩和淡出动画，下方的聊天记录平滑上移填补空白。

2. 聊天主视图与输入区 (Main View & Input)
主视图保持沉浸感，输入区严格遵循生产力工具标准。

视口滚动联动 (ScrollSpy)：当用户在主视图上下滚动查看历史对话时，右侧面板会自动高亮当前视口内最顶部的问题条目，提供清晰的位置感知。

快捷键输入规范：

换行：输入框作为标准文本域，按下 Enter 键纯粹执行原生的换行逻辑，方便用户编写多段落的复杂提示词或粘贴代码。

发送：严格监听 Ctrl + Enter (Windows/Linux) 或 Cmd + Enter (macOS) 的组合键来触发消息发送。

视觉提示：在输入框内部右下角以暗色水印或在“发送”按钮的提示框中，明确标示 ↵ 换行 / ⌘+↵ 发送，降低新用户的学习门槛。

停止生成与回填 (Stop & Refill)：

当 AI 正在生成回复时，发送按钮切换为显眼的“停止”按钮。

用户点击停止后，AI 输出立即中断。

防呆恢复：系统自动将用户刚刚发送的提示词重新填入底部的输入框，并自动让输入框获取光标焦点，用户可以直接在此基础上进行修改后重新发送。

二、 架构与数据流设计 (Architecture & Data Flow)
在彻底剥离代码细节后，我们需要确保前端状态管理的单一事实来源，以支撑面板与主视图的双向联动。

1. 数据模型扩展
在核心消息数据模型中，除了基础的唯一标识、角色（用户/AI）、文本内容和时间戳外，需要引入两个关键的状态字段：

星标状态：用于标识该问答对是否被收藏。

软删除标记：建议采用逻辑删除（而非物理销毁数据）。这不仅便于未来可能增加的“撤销删除”功能，也能保证本地数据库结构的稳定性。

2. 全局状态管理机制
由于右侧面板和主视图是平级的兄弟组件或跨层级组件，必须引入全局状态中心来统筹。

核心状态：维护一个包含所有历史消息的完整数组。

衍生状态 (视图层)：

大纲列表：通过过滤核心数组，剔除“AI回复”和“已软删除”的消息，提取出仅供右侧面板渲染的纯问题列表。

LLM 上下文：在用户发起新提问时，从核心数组中动态过滤掉所有带有“软删除标记”的节点，生成一个干净的上下文数组发送给后端接口。这从根本上实现了精简上下文的需求。

状态变更逻辑：

处理删除：当在右侧面板点击删除时，状态管理器根据传入的问题标识，将该问题及其紧随其后的 AI 回复的“软删除标记”同时设为真。衍生状态会自动更新，从而驱动 UI 响应。

3. 核心机制设计
网络请求中断：利用浏览器原生的中止控制器机制。发送请求时生成一个信号令牌，点击“停止”时触发中止信号，网络层捕获到中止异常后，立刻触发 UI 层的“输入框回填”与“光标聚焦”指令。

平滑滚动定位：通过记录每条消息的唯一标识作为 DOM 锚点，右侧面板点击时，调用原生滚动 API 让主视图平滑滚动到指定锚点位置。

三、 性能优化考量 (Performance Considerations)
按需渲染与缓存：随着对话变长，状态变更（如输入框打字）不应引发全量列表的重新计算。需要利用前端框架的缓存机制，确保右侧面板只在增删消息或星标状态改变时才重新渲染。

滚动侦听防抖：主视图滚动同步到右侧面板高亮的功能，由于滚动事件触发极其频繁，必须在事件派发层增加防抖处理，避免过度消耗浏览器性能导致页面卡顿。